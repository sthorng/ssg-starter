{
  "name": "11ty-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf public",
    "dev:tailwind": "npx tailwindcss -i src/styles/tailwind.css -o public/styles/main.css --watch",
    "dev:eleventy": "npx @11ty/eleventy --serve",
    "build:tailwind": "npx tailwindcss -i src/styles/tailwind.css -o public/styles/main.css --minify",
    "build:eleventy": "ELEVENTY_PRODUCTION=true npx @11ty/eleventy",
    "dev": "npm run clean & concurrently npm:dev:*",
    "build": "NODE_ENV=production npm run clean & concurrently npm:build:*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "concurrently": "^6.5.1",
    "rimraf": "^3.0.2",
    "tailwindcss": "^3.0.7"
  }
}
